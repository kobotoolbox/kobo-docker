# Default backup schedule is weekly at 01:00 AM UTC on Sunday. For help setting 
#   a backup schedule, use a `cron` schedule generator (e.g. crontab.guru). To 
#   disable, comment out the line.
MONGO_BACKUP_SCHEDULE=0 1 * * 0
